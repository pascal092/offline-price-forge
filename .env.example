# Supabase-Anbindung (server-only) — in Vercel unter
# Project Settings → Environment Variables setzen.
#
# WICHTIG: Der service_role-Key ist geheim. Er wird ausschliesslich
# serverseitig verwendet (src/lib/supabase.server.ts + createServerFn) und
# gelangt nie in den Browser. NICHT den anon-Key hier eintragen.

SUPABASE_URL=https://veiciuhbpekaeabyvhpx.supabase.co
SUPABASE_SERVICE_ROLE_KEY=dein_service_role_key
